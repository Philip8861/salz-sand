# Backend-Umgebung (lokal)
# Kopiere diese Datei nach .env:  cp .env.example .env  (bzw. unter Windows: copy .env.example .env)
# Dann die Werte unten anpassen.

# PostgreSQL – Verbindungs-URL zur Datenbank
# Beispiele:
#   Lokal (z.B. Docker):  postgresql://postgres:postgres@localhost:5432/salzsand?schema=public
#   Railway/Cloud:       URL aus dem Railway-Dashboard (PostgreSQL Service → Connect → DATABASE_URL)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public

# Geheimer Schlüssel für JWT (mind. 32 Zeichen) – bitte eigenen Wert verwenden
JWT_SECRET=dein-mindestens-32-zeichen-langer-geheimer-schluessel

# Optional: Port (Standard 3000)
# PORT=3000

# Optional: Frontend-URL für CORS (Standard für lokal: http://localhost:5173)
# FRONTEND_URL=http://localhost:5173
